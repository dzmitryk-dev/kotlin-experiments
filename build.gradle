// Top-level build file for composite build coordination
plugins {
    alias(libs.plugins.kotlin) apply false
}

// Root project doesn't need much configuration in a composite build
// Each included build is self-contained and independent
